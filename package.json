{
  "name": "swearjar-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for the Swear Jar application",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network baseMainnet",
    "test:contract": "hardhat run scripts/test-contract.ts",
    "verify:sepolia": "hardhat verify --network baseSepolia",
    "verify:mainnet": "hardhat verify --network baseMainnet"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "base",
    "smart-contracts"
  ],
  "author": "Swear Jar Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "@types/node": "^20.19.23",
    "dotenv": "^16.6.1",
    "hardhat": "^2.26.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "ethers": "^6.11.1"
  }
}
