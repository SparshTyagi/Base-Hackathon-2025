# Farcaster Monitoring Agent - Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# REQUIRED API KEYS
# ===========================================

# Neynar API Key - Required for Farcaster data access
# Get your key from: https://neynar.com/
NEYNAR_API_KEY=your_neynar_api_key_here

# OpenRouter API Key - Required for LLM functionality
# Get your key from: https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# Backend Integration
# URL of the TypeScript backend webhook endpoint (optional)
# If set, violations will be sent to this endpoint in real-time
# Example: http://localhost:8080 or https://your-backend.railway.app
BACKEND_WEBHOOK_URL=

# Database Configuration
# Path to the SQLite database file (default: violations.db)
DATABASE_PATH=violations.db

# LLM Model Configuration
# Default model for LLM requests (default: nvidia/nemotron-nano-9b-v2:free)
DEFAULT_MODEL=nvidia/nemotron-nano-9b-v2:free

# Fallback models if the default fails (comma-separated)
# (default: openai/gpt-oss-20b:free)
FALLBACK_MODELS=openai/gpt-oss-20b:free

# LLM Request Behavior
# Timeout for LLM requests in seconds (default: 45)
LLM_REQUEST_TIMEOUT_S=45

# Maximum retries at the client level (default: 0)
LLM_MAX_RETRIES=0

# Number of attempts per model before falling back (default: 3)
LLM_ATTEMPTS_PER_MODEL=3

# Retry delays in seconds (comma-separated, default: 15,20)
LLM_RETRY_DELAYS_S=15,20

# ===========================================
# USAGE INSTRUCTIONS
# ===========================================
#
# 1. Copy this file to .env in the same directory
# 2. Replace the placeholder values with your actual API keys
# 3. Optional: Adjust other settings as needed
# 4. The application will automatically load these values
#
# Example:
# NEYNAR_API_KEY=sk_1234567890abcdef
# OPENROUTER_API_KEY=sk-or-v1-1234567890abcdef
#
# ===========================================